sentinel_project:
  outputs:
    dev:
      type: bigquery
      method: service-account
      project: sentinel-486707
      dataset: sentinel_refined
      threads: 4
      keyfile: ./path/to/your/sa-key.json # Locally, use a key; in CI/CD, we'll use env vars
      location: US
      priority: interactive
  target: dev